{
  "name": "@mattb.tech/graphql-api-oauth-lib",
  "version": "1.0.0",
  "description": "Lib for working with oauth table",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run typescript",
    "fix": "prettier --write '**/*' -u --ignore-path '../.gitignore'",
    "check": "tsc --noEmit && prettier --check '**/*' -u --ignore-path '../.gitignore'",
    "typescript": "tsc",
    "typescript:watch": "tsc --watch"
  },
  "author": "Matt Benton <m@mattb.tech>",
  "license": "UNLICENSED",
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.449.0",
    "@aws-sdk/lib-dynamodb": "^3.449.0",
    "axios": "^1.8.2",
    "date-fns": "^2.30.0",
    "envalid": "^8.0.0"
  }
}
